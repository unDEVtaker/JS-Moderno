const producto = 'Monitor 20';
const precio = '30 USD'

console.log(producto.concat(precio));
//console.log(producto.concat('en descuento'));

//console.log(producto + "concatenado con texto");

console.log(`el producto ${producto} tiene un precio de${precio}`)